name: Trigger Browser Tests on New Tag Release

on:
  release:
    types: [published]

permissions:
  contents: read
  actions: read

jobs:
  trigger-browser-tests:
    uses: FalkorDB/falkordb-browser/.github/workflows/playwright.yml@main
    secrets:
      PAT_TOKEN: ${{ secrets.RUN_BROWSER_TESTS }}