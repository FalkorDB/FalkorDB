name: imdb-MSF
db_config:
  dataset: datasets/imdb.rdb
  dataset_load_timeout_secs: 180
parameters:
  graph: imdb
  num_clients: 64
  num_requests: 120000
  queries:
    - query: 'CALL algo.MSF() YIELD nodes RETURN count(nodes) as tree_size'
      ratio: 1.0
